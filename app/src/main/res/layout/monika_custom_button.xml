<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <ImageView
        android:id="@+id/btn_bg_image_view"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:whRatio="5.9433962264" />

    <TextView
        android:id="@+id/btn_title_view"
        android:layout_width="wrap_content"
        android:layout_height="0dp"
        android:gravity="center"
        android:includeFontPadding="false"
        android:textSize="14sp"
        app:layout_constraintBottom_toBottomOf="@+id/btn_bg_image_view"
        app:layout_constraintEnd_toEndOf="@+id/btn_bg_image_view"
        app:layout_constraintStart_toStartOf="@+id/btn_bg_image_view"
        app:layout_constraintTop_toTopOf="@+id/btn_bg_image_view"
        tools:text="同意"
        tools:textColor="#C4FF05" />


</androidx.constraintlayout.widget.ConstraintLayout>