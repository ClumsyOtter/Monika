<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content">

    <!-- 点赞图标 -->
    <ImageView
        android:id="@+id/iv_common_icon"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:src="@drawable/monika_post_detail_like_icon_unselected"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:ignore="ContentDescription"
        tools:src="@drawable/monika_post_detail_like_icon_unselected" />

    <!-- 点赞数量 -->
    <TextView
        android:id="@+id/tv_common_count"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="@dimen/dimen_5dp"
        android:text="0"
        android:textColor="@color/text_808080"
        android:textSize="12sp"
        app:layout_constraintBottom_toBottomOf="@id/iv_common_icon"
        app:layout_constraintStart_toEndOf="@+id/iv_common_icon"
        app:layout_constraintTop_toTopOf="@id/iv_common_icon"
        tools:text="10" />

</androidx.constraintlayout.widget.ConstraintLayout>

